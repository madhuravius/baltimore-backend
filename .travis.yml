language: python
dist: xenial
python: "3.7"
sudo: false
addons:
  apt:
    packages:
    - gdal-bin
    - libgdal-dev
install:
  - pip install -r requirements.txt
before_script:
  - psql -c "CREATE DATABASE codeforbaltimore;" -U postgres
script:
  - make lint
  - make coverage
services:
  - postgresql
